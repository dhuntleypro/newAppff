// import React from 'react';
// import { View, Text, Button } from 'react-native';
// import { ClientStoreProvider, useClientStore } from '@/contexts/ClientStoreContext'; // Adjust import path as necessary

// const SomeComponent = () => {
//   // const { store, addStore, removeStore, isLoading, error } = useClientStore();


//   const handleRemoveStore = () => {
//     removeStore();
//   };

//   return (
//     <View>
//       <Text>{isLoading ? 'Loading...' : store?.store_name ?? 'No Store Selected'}</Text>
//       {error && <Text>Error: {error}</Text>}
//       <Button title="Add Store" onPress={() => console.log("YOooooo")} />
//       <Button title="Remove Store" onPress={handleRemoveStore} />
//     </View>
//   );
// };

// export default SomeComponent;
